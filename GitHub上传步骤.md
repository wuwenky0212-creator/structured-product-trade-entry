# 📤 GitHub 上传步骤详解

## 步骤 1：在 GitHub 上创建仓库

### 1.1 登录 GitHub
1. 访问 [github.com](https://github.com)
2. 如果没有账号，点击 "Sign up" 注册（免费）
3. 如果已有账号，点击 "Sign in" 登录

### 1.2 创建新仓库
1. 登录后，点击右上角的 **"+"** 号（加号图标）
2. 在下拉菜单中选择 **"New repository"**（新建仓库）

3. 填写仓库信息：
   - **Repository name**（仓库名称）：输入 `structured-product-trade-entry`（或你喜欢的名字）
   - **Description**（描述）：输入 `结构化产品交易录入系统`（可选）
   - **Visibility**（可见性）：
     - 选择 **Public**（公开）- 所有人都能看到
     - 或选择 **Private**（私有）- 只有你能看到
   - **重要**：**不要**勾选以下选项：
     - ❌ Add a README file
     - ❌ Add .gitignore
     - ❌ Choose a license

4. 点击 **"Create repository"**（创建仓库）按钮

### 1.3 记住仓库地址
创建成功后，GitHub 会显示一个页面，上面有仓库的地址，格式类似：
```
https://github.com/你的用户名/structured-product-trade-entry.git
```
**把这个地址记下来，后面要用！**

---

## 步骤 2：在本地项目初始化 Git

### 2.1 打开命令行
在项目文件夹 `D:\cursor workspace\Tradebooking` 中：
- 右键点击文件夹，选择 "在终端中打开"
- 或者在 Cursor 中打开终端（Terminal）

### 2.2 执行 Git 命令

**⚠️ 注意：如果使用 PowerShell 执行 npm 命令失败，请使用 `cmd /c` 前缀**

按照以下顺序执行命令：

#### ① 初始化 Git 仓库
```bash
git init
```

#### ② 添加所有文件到暂存区
```bash
git add .
```

#### ③ 创建第一次提交
```bash
git commit -m "Initial commit: 结构化产品交易录入系统"
```

#### ④ 添加远程仓库地址
**⚠️ 重要：将下面命令中的内容替换为你的实际信息**
- 将 `你的用户名` 替换为你的 GitHub 用户名
- 将 `structured-product-trade-entry` 替换为你创建的仓库名（如果不一样）

```bash
git remote add origin https://github.com/你的用户名/structured-product-trade-entry.git
```

**示例**：如果你的用户名是 `zhangsan`，仓库名是 `trade-entry`，那么命令应该是：
```bash
git remote add origin https://github.com/zhangsan/trade-entry.git
```

#### ⑤ 设置主分支名称为 main
```bash
git branch -M main
```

#### ⑥ 推送代码到 GitHub
```bash
git push -u origin main
```

---

## 步骤 3：处理认证问题

### 如果提示需要登录：

#### 方法 A：使用 Personal Access Token（推荐）

1. **生成 Token**：
   - 访问：https://github.com/settings/tokens
   - 或者：GitHub 右上角头像 → Settings → Developer settings → Personal access tokens → Tokens (classic)
   - 点击 **"Generate new token"** → **"Generate new token (classic)"**

2. **配置 Token**：
   - **Note**（备注）：输入 `Railway Deployment`（随便写个名字）
   - **Expiration**（过期时间）：选择 `90 days` 或 `No expiration`
   - **Select scopes**（选择权限）：勾选以下选项：
     - ✅ `repo`（完整仓库权限，包括私有仓库）

3. **生成并复制 Token**：
   - 点击 **"Generate token"** 按钮
   - **重要**：立即复制生成的 token（格式类似：`ghp_xxxxxxxxxxxx`），只显示一次！

4. **使用 Token 推送**：
   - 当 `git push` 提示输入用户名时，输入你的 GitHub 用户名
   - 当提示输入密码时，**粘贴刚才复制的 token**（不是密码！）

#### 方法 B：使用 GitHub CLI（可选）

如果安装了 GitHub CLI：
```bash
gh auth login
```

#### 方法 C：配置 SSH 密钥（可选，更安全）

如果你想使用 SSH 方式（推荐用于长期使用）：

1. **生成 SSH 密钥**（如果还没有）：
```bash
ssh-keygen -t ed25519 -C "your_email@example.com"
```
按 Enter 使用默认路径，可以设置密码（或留空）

2. **复制公钥内容**：
```bash
cat ~/.ssh/id_ed25519.pub
```
或者 Windows 路径：
```bash
type %USERPROFILE%\.ssh\id_ed25519.pub
```

3. **添加到 GitHub**：
   - 访问：https://github.com/settings/ssh/new
   - 将复制的公钥内容粘贴进去
   - 点击 "Add SSH key"

4. **修改远程仓库地址为 SSH**：
```bash
git remote set-url origin git@github.com:你的用户名/structured-product-trade-entry.git
```

5. **重新推送**：
```bash
git push -u origin main
```

---

## 验证上传是否成功

1. 访问你的 GitHub 仓库页面：
   ```
   https://github.com/你的用户名/structured-product-trade-entry
   ```

2. 你应该能看到：
   - ✅ 所有项目文件都在列表中
   - ✅ README.md（如果有）
   - ✅ package.json
   - ✅ src 文件夹等

3. 点击文件名可以查看文件内容

---

## 常见问题解决

### ❌ 问题 1：`git push` 提示 "remote: Support for password authentication was removed"

**原因**：GitHub 已经不支持使用密码登录

**解决**：使用 Personal Access Token（见上面的步骤 3 方法 A）

### ❌ 问题 2：`git push` 提示 "Permission denied"

**原因**：没有权限或认证失败

**解决**：
1. 检查仓库地址是否正确
2. 确认使用的是 Token 而不是密码
3. 尝试重新生成 Token

### ❌ 问题 3：`git add .` 后文件太多或太慢

**原因**：可能包含了 `node_modules` 等不需要上传的文件

**解决**：检查 `.gitignore` 文件是否包含了：
```
node_modules
dist
```

### ❌ 问题 4：PowerShell 执行 Git 命令失败

**解决**：使用 `cmd /c` 前缀：
```bash
cmd /c git init
cmd /c git add .
cmd /c git commit -m "Initial commit"
```

或者切换到 cmd：
```bash
cmd
```
然后在 cmd 中执行 Git 命令

---

## ✅ 完成检查清单

完成以下所有步骤后，你的代码就成功上传到 GitHub 了：

- [ ] 已在 GitHub 创建仓库
- [ ] 已执行 `git init`
- [ ] 已执行 `git add .`
- [ ] 已执行 `git commit -m "..."`
- [ ] 已添加远程仓库地址 `git remote add origin ...`
- [ ] 已设置分支 `git branch -M main`
- [ ] 已成功推送 `git push -u origin main`
- [ ] 在 GitHub 网页上能看到所有文件

---

## 🎯 下一步

上传成功后，就可以继续：
- **步骤 2：部署到 Railway**（查看 `DEPLOYMENT_GUIDE.md`）

---

**需要帮助？** 如果在任何步骤遇到问题，请告诉我具体的错误信息，我会帮你解决！

